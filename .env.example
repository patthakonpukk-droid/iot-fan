# ==============================================================
#  iot-fan — Environment Variables Template
#  วิธีใช้: คัดลอกไฟล์นี้เป็น .env แล้วใส่ค่าจริง
#
#    cp .env.example .env
#
#  จากนั้นแก้ค่าแต่ละบรรทัดให้ตรงกับของคุณ
#  ไฟล์ .env จะถูก .gitignore ไว้ ไม่ถูก commit ขึ้น Git
# ==============================================================

# --- Supabase (ดูได้จาก Project Settings > API ใน Supabase Dashboard) ---
SUPABASE_URL=https://xxxxxxxxxxxxxxxxxxxx.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# --- Web Password (รหัสผ่านสำหรับเข้าหน้าเว็บ) ---
ACCESS_PASSWORD=your-web-password

# --- JWT Secret
#     สร้างได้ด้วยคำสั่ง:
#       node -e "console.log(require('crypto').randomBytes(48).toString('base64url'))"
#     หรือ:
#       python3 -c "import secrets; print(secrets.token_urlsafe(48))"
# ---
JWT_SECRET=your-random-jwt-secret-at-least-32-characters-long

# --- Device Secret (ต้องตรงกับ deviceSecret ใน esp8266.ino)
#     สร้างได้ด้วยคำสั่ง:
#       node -e "console.log(require('crypto').randomBytes(18).toString('base64url'))"
#     หรือ:
#       python3 -c "import secrets; print(secrets.token_urlsafe(18))"
# ---
DEVICE_SECRET=your-device-secret-key
